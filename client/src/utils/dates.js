// COPY/PASTE HERE, STRETCH FEATURE:
// Add helper functions for formatting event times and countdowns.
// Event.jsx already calls these functions, so fill them in when you are ready.

const formatTime = async (time) => {
    return time || ''
}

const formatRemainingTime = async (remaining) => {
    return remaining || ''
}

const formatNegativeTimeRemaining = async () => {
    // COPY/PASTE HERE:
    // Add styling logic for events that already passed.
}

export default {
    formatTime,
    formatRemainingTime,
    formatNegativeTimeRemaining
}
